services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    image: shubham-portfolio:latest
    ports:
      - "3000:3000"
    restart: always
    environment:
      - NODE_ENV=production
